获取Token
更新时间: 2026-03-11 09:18
功能介绍
在使用API客户端方式调用Connect API的接口前，需要通过华为开放平台进行鉴权，并获取认证通过后的Token。
接口原型
承载协议
HTTPS POST
接口方向
开发者服务器 -> 华为服务器
接口URL
https://connect-api.cloud.huawei.com/api/oauth2/v1/token
数据格式
请求：Content-Type: application/json
响应：Content-Type: application/json
请求参数
请求参数以JSON格式传入，包含参数如下。
参数名称
必选(M)/可选(O)
数据类型
参数说明
grant_type
M
String(256)
固定传入“client_credentials”。
client_id
M
String(256)
客户端ID，即创建API客户端中生成的“ID”。
client_secret
M
String(2048)
客户端密钥，即创建API客户端中生成的“密钥”。
请求示例
POST /api/oauth2/v1/token
Host: connect-api.cloud.huawei.com
Content-Type: application/json
{
"grant_type":"client_credentials",
"client_id":"26********20",
"client_secret":"************************"
}
响应参数
返回值为JSON格式的字符串，包含参数如下。
参数名称
必选(M)/可选(O)
数据类型
参数说明
access_token
O
String
认证Token，用于AppGallery Connect API接口调用。
此参数只在获取成功时返回。
expires_in
O
Long
access_token的有效期，单位秒。您需要在过期时间到达时重新调用本接口获取新的access_token。
有效期为48小时，如果在有效期内再次调用接口获取access_token时，新老access_token都是有效的。
此参数只在获取成功时返回。
ret
O
String(100)
获取Token失败时的错误信息，包含错误码及描述信息的JSON字符串，格式为{"code":retcode, "msg": "description"}，retcode为错误码，description为错误码描述信息。
响应示例
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
{
"access_token": "eyJhbGciOiJIUzU****************",
"expires_in": 172800
}
调用示例
publicstaticStringgetToken(String domain, String clientId, String clientSecret) {
String token = null;
try {
HttpPost post = newHttpPost(domain + "/oauth2/v1/token");
JSONObject keyString = newJSONObject();
keyString.put("client_id", "18893***83957248");
keyString.put("client_secret", "B15B497B44E080EBE2C4DE4E74930***52409516B2A1A5C8F0FCD2C579A8EB14");
keyString.put("grant_type", "client_credentials");
StringEntity entity = newStringEntity(keyString.toString(), Charset.forName("UTF-8"));
entity.setContentEncoding("UTF-8");
entity.setContentType("application/json");
post.setEntity(entity);
CloseableHttpClient httpClient = HttpClients.createDefault();
HttpResponse response = httpClient.execute(post);
int statusCode = response.getStatusLine().getStatusCode();
if (statusCode == HttpStatus.SC_OK) {
BufferedReader br =
newBufferedReader(newInputStreamReader(response.getEntity().getContent(), Consts.UTF_8));
String result = br.readLine();
JSONObjectobject = JSON.parseObject(result);
token = object.getString("access_token");
}
post.releaseConnection();
httpClient.close();
} catch (Exception e) {
}
return token;
}
Postman调试
您可以使用Postman在线调试此接口。
查询应用包名对应的appid
更新时间: 2025-08-06 18:02
功能介绍
此接口用于根据应用包名查询对应的应用ID，支持一次查询多个应用包名。接口调用者的角色：账号持有者、管理员、APP管理员、运营。
接口原型
承载协议
HTTPS GET
接口方向
开发者服务器 -> 华为服务器
接口URL
https://connect-api.cloud.huawei.com/api/publish/v2/appid-list
数据格式
请求：Content-Type: application/json
响应：Content-Type: application/json
请求参数
Header
说明
API客户端方式和OAuth客户端方式的区别请参见使用入门。
API客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
client_id
M
String
客户端ID，获取方法参考创建API客户端。
Authorization
M
String
认证信息，格式为“Authorization: Bearer ${access_token}”。access_token为获取Token中获取的access_token。
OAuth客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
teamId
M
String
开发者所在团队的团队ID，可通过获取团队列表接口获取。
oauth2Token
M
String
认证信息，传入获取用户授权码中获取的Access Token。
Query
参数名称
必选(M)/可选(O)
类型
参数说明
packageName
M
String(4096)
需要查询的应用包名，多个包名以逗号分隔，最多支持50个。
packageTypes
O
String(32)
通过软件包类型过滤应用，只查询指定软件包类型的应用。
取值范围：
1：APK
2：RPK
8：EXE（Windows应用）
包含多个软件包类型时使用英文逗号分隔。
pcVersionName
O
String(255)
通过软件包版本号过滤Windows应用。
只有packageTypes为8，且只有一个Windows应用时有效。
查询Windows应用会校验传入的版本号是否大于等于现有的最新软件包版本号，如果大于等于，则正常返回，否则报错版本号低于现有版本。
请求示例
GET /api/publish/v2/appid-list?packageName=com.huawei.xxxx HTTP/1.1
Host: connect-api.cloud.huawei.com
client_id: 41*******7168
Authorization: Bearer *******
响应参数
参数名称
必选(M)/可选(O)
类型
参数说明
ret
M
String(100)
包含返回码及描述信息的JSON字符串，格式为{"code":retcode, "msg": "description"}，retcode为返回码，description为返回码描述信息。返回码请参见错误码。
appids
O
List<Pair>
应用包名对应的AppId对象的数组。
Pair
参数名称
必选(M)/可选(O)
类型
参数说明
key
O
String(128)
应用名称。
value
O
String(128)
应用包名对应的应用ID。
响应示例
{
"ret": {
"code": 0,
"msg": "success"
},
"appids": [
{
"key": "PhotoPlaza",
"value": "10****057"
}
]
}
调用示例
publicstaticAppId[] queryAppId(String domain, String client_id, String token, String packageNames) {
HttpGet get = newHttpGet(domain + "/publish/v2/appid-list?packageName=" + packageNames);
get.setHeader("client_id", client_id);
get.setHeader("Authorization", "Bearer " + token);
try {
CloseableHttpClient httpClient = HttpClients.createDefault();
CloseableHttpResponse httpResponse = httpClient.execute(get);
int statusCode = httpResponse.getStatusLine().getStatusCode();
if (statusCode == HttpStatus.SC_OK) {
BufferedReader br =
newBufferedReader(newInputStreamReader(httpResponse.getEntity().getContent(), Consts.UTF_8));
String result = br.readLine();
JSONObjectobject = JSON.parseObject(result);
System.out.println(object.get("ret"));
AppId[] appids = (AppId[]) object.get("appids");
return appids;
}
} catch (Exception e) {
e.printStackTrace();
}
returnnull;
}
Postman调试
您可以使用Postman在线调试此接口。
查询应用信息
更新时间: 2025-11-21 17:52
功能介绍
此接口用于根据指定语言和应用ID查询应用的详细信息。接口调用者的角色：账号持有者、管理员、APP管理员、运营。
接口原型
承载协议
HTTPS GET
接口方向
开发者服务器 -> 华为服务器
接口URL
https://connect-api.cloud.huawei.com/api/publish/v2/app-info
数据格式
请求：Content-Type: application/json
响应：Content-Type: application/json
请求参数
Header
说明
API客户端方式和OAuth客户端方式的区别请参见使用入门。
API客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
client_id
M
String
客户端ID，获取方法参考创建API客户端。
Authorization
M
String
认证信息，格式为“Authorization: Bearer ${access_token}”。access_token为获取Token中获取的access_token。
OAuth客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
teamId
M
String
开发者所在团队的团队ID，可通过获取团队列表接口获取。
oauth2Token
M
String
认证信息，传入获取用户授权码中获取的Access Token。
Query
参数名称
必选(M)/可选(O)
类型
参数说明
appId
M
String
需要查询的应用ID。
lang
O
String
需要查询的语言，取值参考语言类型。不传该参数则查询全部语言的应用信息。
releaseType
O
Integer
应用发布方式。
取值范围：
1：全网
3：分阶段
默认值：1
请求样例
GET /api/publish/v2/app-info?appId=10*****57 HTTP/1.1
Host: connect-api.cloud.huawei.com
client_id: 41*****7168
Authorization: Bearer ******
响应参数
参数名称
必选(M)/可选(O)
类型
参数说明
ret
M
String(100)
包含返回码及描述信息的JSON字符串，格式为{"code":retcode, "msg": "description"}，retcode为返回码，description为返回码描述信息。返回码请参见错误码。
appInfo
O
AppInfo
应用基本信息。
auditInfo
O
AuditInfo
审核意见信息。
languages
M
List<LanguageInfo>
包含多种语言的应用信息。
如果查询时传递了语言类型，则返回当前语言的应用信息。
如果查询时未传递语言类型，则返回所有语言的应用信息。
如果查询时设置了异常的语言数据（例如不存在的语言类型），则返回空数组。
phasedReleaseInfo
O
PhasedReleaseInfo
分阶段发布信息。
AppInfo
参数名称
必选(M)/可选(O)
类型
参数说明
releaseState
O
Integer(4)
应用状态。
releaseType为1时，状态含义如下。各状态间的转换请参见全网版本状态流转说明。
0：已上架
1：上架审核不通过
2：已下架（含强制下架）
3：待上架，预约上架
4：审核中
5：升级审核中
6：申请下架
7：草稿
8：升级审核不通过
9：下架审核不通过
10：应用被开发者下架
11：撤销上架
12：预审中
13：预审不通过
defaultLang
O
String(64)
应用的默认语言，取值参见语言类型。
parentType
M
Integer(4)
应用的一级分类，固定ID，取值参见应用游戏分类。
childType
M
Integer(4)
应用的二级分类，固定ID，取值参见应用游戏分类。
grandChildType
M
Integer(4)
应用的三级分类，固定ID，取值参见应用游戏分类。
privacyPolicy
M
String(255)
隐私政策声明的网页地址。
该网址会在应用的详情页面添加隐私政策跳转，可帮助用户清楚地了解您如何处理敏感的用户数据和设备数据。
隐私政策必须完整说明您的应用如何收集、使用和分享用户数据，包含但不限于如下情况建议提供：
面向儿童的App。
包含账户注册或需要访问用户的现有账户，或由法律另行规定。
对于收集用户或设备相关数据的App。
appNetType
O
Integer(1)
应用联网类型。
取值范围：
1：单机
2：网游
说明
此参数只支持游戏类应用。
isFree
O
Integer(1)
应用付费类型。
取值范围：
1：免费
0：付费
price
O
String(256)
应用价格，格式为{"countryCode":"price"}。countryCode为国家码，price为应用的价格。
priceDetail
O
String(8192)
应用在不同国家或地区的价格，json数组格式，不同国家和地区之间使用逗号分隔，例如：[{"country":"CN","currency":"CNY","price":"18.00"},{"country":"DE","currency":"EUR","price":"2.50"}]。
其中，country为国家码，currency为国家码所对应的币种，price为应用价格。
publishCountry
O
String(256)
应用发布国家的国家码，多个国家以英文逗号分隔，例如：“CN,DE,MC”。
contentRate
O
String(256)
应用分级，Json格式，例如{"HW":"3+"}。
isAppForcedUpdate
O
Integer(1)
应用是否强制升级。
取值范围：
0：不强制
1：强制
sensitivePermissionDesc
O
String(256)
敏感权限说明。
hispaceAutoDown
O
Integer(1)
是否允许华为应用市场抓包。
取值范围：
0：不允许
1：允许
appTariffType
O
String(16)
应用内付费道具类型。
取值范围：
1：激活收费
2：道具收费
3：关卡收费
4：购买虚拟币
5：部分章节收费（图书阅读类）
6：其他的
7：课程收费
8：会员收费
返回多个表示具备多个类型，例如“1,2,3”。
publicationNumber
O
String(50)
版号信息。
cultureRecordNumber
O
String(50)
文化部备案编号。
developerWebsite
O
String(255)
开发者官网。
developerNameCn
O
String(64)
开发者中文名称。
developerNameEn
O
String(64)
开发者英文名称。
elecCertificateUrl
O
String(256)
电子版权证书PDF地址。
certificateURLs
M
String(256)
应用版权证书或代理证书图片地址列表，逗号分隔，最多5个。
publicationURLs
O
String(1024)
版号证明，逗号分隔的地址，可以为图片或者PDF，最多3个。
cultureRecordURLs
O
String(256)
文化部备案证明地址，图片或者PDF。
updateTime
O
String(32)
更新时间，UTC时间格式：yyyy-mm-dd hh:mm:ss。
versionNumber
O
String(32)
版本号。
versionCode
O
Integer(64)
对应软件包中的versionCode。
versionId
O
String(32)
版本ID。
onShelfVersionNumber
O
String(32)
在架版本版本号。
onShelfVersionCode
O
Integer(64)
在架版本versionCode，对应软件包中的versionCode。
onShelfVersionId
O
String(32)
在架版本版本ID。
shareLink
O
String(1024)
分享链接。
familyShareTag
O
Integer(1)
是否可以家庭共享。0：不可以；1：可以。只有分发区域包含中国大陆的应用返回1。
deviceTypes
M
List<DeviceTypeInfo>
应用的设备类型信息，数组长度不超过6。
webGameFlag
O
Integer
该应用是否为网页游戏。
取值范围：
0：否
1：是
privacyRightsUrl
O
String(256)
隐私权利网址，即关于您的用户实施其权利的网站地址，如用户删除、修改、导出其个人数据的入口。
isSignedCommitment
M
Integer
是否签署合规承诺函。
说明
当前仅元服务涉及签署合规承诺函。
取值范围：
空：表示未签署
1：表示已签署
默认值：空
registeredIdType
O
Integer(32)
备案证件类型。
取值范围：
-1：清空数据，同时也会清空registeredIdNumber的内容。
1：面向企业，备案证件为社会信用
2：面向个人，备案证件为身份证
3：面向机构，备案证件为机构代码
registeredIdNumber
O
String(64)
备案证件号码。
取值范围：
registeredIdType为1时，对应社会信用代码
registeredIdType为2时，对应身份证号码
registeredIdType为3时，对应机构代码
privacyAgreementId
O
String(32)
隐私协议ID。
userAgreementUrl
O
String(512)
用户协议URL。
invitationCount
O
Integer
邀请量。
如果是邀请测试，返回的是参与测试的用户数。
如果是公开测试，返回的是下载安装次数。
downloadCount
O
Integer
下载量。该版本启动下载的累计次数。
全网版本状态流转说明
DeviceTypeInfo
参数名称
必选(M)/可选(O)
类型
参数说明
deviceType
M
Integer
设备类型。
取值范围：
4：手机
6：VR
7：手表
8：大屏
9：路由器
10：车机
appAdapters
O
String
适配设备类型，多个类型以英文逗号分隔。
取值范围：
1：荣耀立方（路由器）
4：手机
5：平板
6：VR
7：手表
8：大屏
9：路由器
11：儿童表
12：智能手表
13：带屏音响
LanguageInfo
参数名称
必选(M)/可选(O)
类型
参数说明
lang
O
String(64)
当前语言，取值参考语言类型。
appName
O
String(64)
当前语言的应用名称。
appDesc
O
String(8000)
当前语言的应用描述。
briefInfo
O
String(60)
当前语言的一句话简介。
newFeatures
O
String(1000)
当前语言的新版本简介。
icon
O
String(256)
当前语言的图标地址。
showType
O
Integer(1)
当前语言的截图展现方式。
取值范围：
0：竖屏
1：横屏
videoShowType
O
Integer(1)
当前语言的视频展现方式。
取值范围：
0：竖屏
1：横屏
introPic
O
String(4096)
应用介绍的截图地址，多个地址以逗号隔开，最多不超过10个地址。
introVideo
O
String(4096)
应用的介绍视频地址和视频截图地址，最多5个视频，每个视频必须对应截图。例如：[{"videoUrl":"XXX/app/000/000/375/20160512143416.mp4","frameUrl":"XXX/app/000/000/375/20160512143416.68439491.jpg"},{"videoUrl":"/app/000/000/376/20160512143416.mp4","frameUrl":"/app/000/000/376/20160512143416.68439491.jpg"}]
rcmdPic
O
String(4096)
应用的推荐截图地址，多个地址以逗号隔开，最多不超过10个地址。
rcmdVideo
O
String(4096)
应用的推荐视频地址以及视频截图地址，最多5个视频，每个视频必须对应截图。例如：[{"videoUrl":"XXX/app/000/000/375/20160512143416.mp4","frameUrl":"XXX/app/000/000/375/20160512143416.68439491.jpg"},{"videoUrl":"/app/000/000/376/20160512143416.mp4","frameUrl":"/app/000/000/376/20160512143416.68439491.jpg"}]
deviceMaterials
O
List<DeviceMaterial>
设备相关的素材，包括图片、视频，数组长度不超过6。
应用为多设备时，只显示手机的相关素材信息。应用为单设备时，显示对应设备相关的素材信息。
DeviceMaterial
参数名称
必选（M）/可选（O）
类型
参数说明
deviceType
M
Integer(1)
设备类型。
取值范围：
4：手机
6：VR
7：手表
8：大屏
9：路由器
10：车机
appIcon
O
String(256)
应用图标下载地址。
screenShots
O
String(2560)
应用截屏地址，多个地址以英文逗号隔开，最多不超过10个地址。
banner
O
String(255)
高清大图地址。
showType
O
Integer(1）
截图展现方式。
取值范围：
0：竖屏
1：横屏
vrCover
O
String(255)
VR封面图片下载地址。
vrShots
O
String(1280)
VR应用截屏地址，多个地址以英文逗号隔开，最多不超过5个地址。
vrRecomGraphic
O
String(255)
VR推荐图片下载地址。
vrCoverLayeredImage
O
String(512)
VR封面分层图片地址，固定要求两个地址，使用英文逗号分隔。第一个为前景图，第二个为背景图。
vrRecomGraphic4to3
O
String(512)
VR 4:3推荐图片地址，固定要求两个地址，使用英文逗号分隔。第一个为前景图，第二个为背景图。
vrRecomGraphic1to1
O
String(512)
VR 1:1推荐图片地址，固定要求两个地址，使用英文逗号分隔。第一个为前景图，第二个为背景图。
vrApplicationPanorama
O
String(255)
VR应用全景图地址。
introVedios
O
String(1300)
介绍视频，Json字符串格式，含有vedioUrl、frameUrl两个字段，数组长度最多5个。
示例：
"introVedios": ["{\"frameUrl\":\"https://**.cn:18445/FileServer/D11.jpeg\",\"vedioUrl\":\"https://*.*.cn:18445/82C.mp4\"}" ]
rcmdVedios
O
String(1300)
推荐视频，Json字符串格式，含有vedioUrl、frameUrl两个字段。数组长度最多5个。
示例：
"rcmdVedios": ["{\"frameUrl\":\"https://*.*.cn:18445/5D7.jpeg\",\"vedioUrl\":\"https://*.*.cn:18445/97A.mp4\"}"
promoGraphics
O
String(2560)
宣传图片地址，多个地址以英文逗号隔开，最多不超过10个地址。
rcmdPics
O
String(1280)
推荐图片地址，多个地址以英文逗号隔开，最多不超过10个地址。
videoShowType
O
Integer(1)
视频展现方式。
取值范围：
0：竖屏
1：横屏
AuditInfo
参数名称
必选(M)/可选(O)
类型
参数说明
auditOpinion
M
String(1024)
应用整体审核意见。
copyRightAuditResult
O
Integer(4)
版权审核结果。
取值范围：
0：通过
1：不通过
只有在中国大陆地区发布的应用才会返回。
copyRightAuditOpinion
O
String(1024)
版权审核意见。
只有在中国大陆地区发布的应用才会返回。
copyRightCodeAuditResult
O
Integer(4)
版号审核结果。
取值范围：
0：通过
1：不通过
只有在中国大陆地区发布的应用才会返回。
copyRightCodeAuditOpinion
O
String(1024)
版号审核意见。
只有在中国大陆地区发布的应用才会返回。
recordAuditResult
O
Integer(4)
备案审核结果。
取值范围：
0：通过
1：不通过
只有在中国大陆地区发布的应用才会返回。
recordAuditOpinion
O
String(1024)
备案审核意见。
只有在中国大陆地区发布的应用才会返回。
PhasedReleaseInfo
参数名称
必选(M)/可选(O)
类型
参数说明
state
M
String
分阶段发布状态。
取值范围：
SUSPEND：暂停
RELEASE：发布中
CANCEL：取消
DRAFT：草稿
phasedReleaseStartTime
M
String(64)
分阶段发布生效开始时间，UTC时间格式：yyyy-MM-ddTHH:mm:ssZZ，例如“2015-01-01T01:01:01+0800”。
phasedReleaseEndTime
M
String(64)
分阶段发布生效结束时间，UTC时间格式: yyyy-MM-ddTHH:mm:ssZZ，例如“2015-01-01T01:01:01+0800”。
phasedReleasePercent
M
String(10)
分阶段发布百分比，取值范围为0.00到100.00，必须精确到两位小数，不含百分号。
phasedReleaseDescription
M
String(500)
分阶段发布说明。
响应示例
{
"ret": {
"code": 0,
"msg": "success"
},
"appInfo": {
"releaseState": 11,
"defaultLang": "zh-CN",
"parentType": 13,
"childType": 26,
"grandChildType": 10041,
"privacyPolicy": "https://*****.link",
"isFree": 1,
"price": "{\"CN\":\"0\"}",
"priceDetail": "[]",
"publishCountry": "AF,AE,OM,PK,PS,BH,QA,KW,LB,SA,IQ,IL,JO,AU,MO,PG,PF,PH,FJ,KR,KH,CK,LA,MV,MY,AS,BD,MM,NR,NP,JP,LK,SB,TW,TH,TO,VU,BN,HK,SG,NZ,IN,ID,VN,AZ,GE,KZ,KG,MN,TJ,TM,UZ,AM,ALL,CN,AL,IE,EE,AD,AT,BG,BE,IS,BA,PL,DK,DE,RU,FR,FO,VA,FI,GL,NL,ME,CZ,HR,LV,LT,LI,LU,RO,MT,MK,MD,MC,NO,PT,SE,CH,RS,CY,SM,SK,SI,UA,ES,GR,HU,IT,GB,GI,BY,TR,DZ,EG,ET,AO,BJ,BW,BF,BI,GQ,TG,ER,CV,GM,CG,CD,DJ,GN,GW,GH,GA,ZW,CM,KM,CI,KE,LS,LR,LY,RE,RW,MG,MW,ML,YT,MU,MR,MA,MZ,NA,ZA,SS,NE,NG,SL,SN,SC,ST,SZ,SO,TZ,TN,UG,YE,ZM,TD,CF,AR,AW,AI,AG,BB,BS,PY,PA,BR,PR,BO,BZ,DO,EC,GF,CO,CR,GD,GP,GY,HT,AN,HN,KY,MQ,MS,PE,MX,NI,SV,LC,VC,SR,TT,GT,VE,UY,JM,VG,CL,CA",
"contentRate": "{\"HW\":\"3+\"}",
"hispaceAutoDown": 0,
"appTariffType": "6",
"developerNameCn": "****",
"developerNameEn": "****",
"developerAddr": "",
"developerEmail": "",
"developerPhone": "",
"developerWebsite": "",
"certificateURLs": "https://***5819F41A7F216EF87637667B4E33C.jpg",
"publicationURLs": "[\"\"]",
"cultureRecordURLs": "[null]",
"updateTime": "2021-03-26 07:47:39",
"versionNumber": "6.5.0.300",
"familyShareTag": 0,
"deviceTypes": [
{
"deviceType": 4,
"appAdapters": "4,5"
}
]
},
"auditInfo": {
"auditOpinion": ""
},
"languages": [
{
"lang": "en-US",
"appName": "PhotoPlaza-en",
"appDesc": "*****",
"briefInfo": "*****",
"newFeatures": "*****",
"icon": "https://****D.png",
"showType": 0,
"videoShowType": 1,
"introPic": "https://*****D0.jpg",
"deviceMaterials": [
{
"deviceType": 4,
"appIcon": "https://****8D.png",
"screenShots": [
"https://****1B.jpg",
"https://****BA.jpg",
"https://****CA.jpg",
"https://****D0.jpg"
],
"showType": 0,
"vrCoverLayeredImage": [],
"vrRecomGraphic4to3": [],
"vrRecomGraphic1to1": [],
"promoGraphics": [],
"videoShowType": 1
}
]
},
{
"lang": "zh-CN",
"appName": "PhotoPlaza",
"appDesc": "****",
"briefInfo": "****",
"newFeatures": "****",
"icon": "****8D.png",
"showType": 0,
"videoShowType": 1,
"introPic": "https://****D0.jpg",
"rcmdPic": ",,,",
"deviceMaterials": [
{
"deviceType": 4,
"appIcon": "https://****8D.png",
"screenShots": [
"https://****1B.jpg",
"https://****BA.jpg",
"https://****CA.jpg",
"https://****D0.jpg"
],
"showType": 0,
"vrCoverLayeredImage": [],
"vrRecomGraphic4to3": [],
"vrRecomGraphic1to1": [],
"promoGraphics": [],
"rcmdPics": ",,,",
"videoShowType": 1
}
]
}
]
}
调用示例
publicstaticvoidgetAppInfo(String domain, String clientId, String token, String appId, String lang) {
HttpGet get = newHttpGet(domain + "/publish/v2/app-info?appId=" + appId + "&lang=" + lang);
get.setHeader("Authorization", "Bearer " + token);
get.setHeader("client_id", clientId);
try {
CloseableHttpClient httpClient = HttpClients.createDefault();
CloseableHttpResponse httpResponse = httpClient.execute(get);
int statusCode = httpResponse.getStatusLine().getStatusCode();
if (statusCode == HttpStatus.SC_OK) {
BufferedReader br =
newBufferedReader(newInputStreamReader(httpResponse.getEntity().getContent(), Consts.UTF_8));
String result = br.readLine();
JSONObjectobject = JSON.parseObject(result);
System.out.println(object.get("ret"));
AppInfo appInfo = (AppInfo) object.get("appInfo");
AuditInfo auditInfo = (AuditInfo) object.get("auditInfo");
LangInfo[] langInfos = (LangInfo[]) object.get("languages");
}
} catch (Exception e) {
}
}
获取文件上传地址
更新时间: 2026-05-26 15:34
功能介绍
上传文件至AppGallery Connect前需要先调用此接口获取上传地址，包括图片、视频、PDF等文件。接口调用者的角色：账号持有者、管理员、APP管理员、运营。
此接口当前支持获取上传地址的应用软件包格式包括APK、AAB、RPK、EXE（Windows应用软件包）。
接口原型
承载协议
HTTPS GET
接口方向
开发者服务器 -> 华为服务器
接口URL
https://connect-api.cloud.huawei.com/api/publish/v2/upload-url/for-obs
数据格式
请求：Content-Type: application/json
响应：Content-Type: application/json
请求参数
Header
说明
本接口支持使用Service Account方式、API客户端方式和OAuth客户端方式，区别请参见使用入门。
Service Account方式：
参数名称
必选(M)/可选(O)
类型
参数说明
Authorization
M
String
认证信息，格式为“Authorization: Bearer ${JWT}”。JWT为通过Service Account方式获取授权中获取的鉴权令牌。
API客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
client_id
M
String
客户端ID，获取方法参考创建API客户端。
Authorization
M
String
认证信息，格式为“Authorization: Bearer ${access_token}”。access_token为获取Token中获取的access_token。
OAuth客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
teamId
M
String
开发者所在团队的团队ID，可通过获取团队列表接口获取。
oauth2Token
M
String
认证信息，传入获取用户授权码中获取的Access Token。
Query
参数名称
必选(M)/可选(O)
类型
参数说明
appId
M
String(32)
应用ID。
fileName
M
String(256)
原始文件名，需包含文件后缀。主要用于在生成上传地址时获取原始文件后缀。
sha256
O
String(64)
文件内容的SHA256哈希值，如果传入此参数，文件在上传到文件服务器后会执行完整性校验。如果不传则不做校验。
contentLength
M
Integer
文件内容的大小，单位：字节。
releaseType
O
Integer
发布方式。
取值范围：
1：全网发布。
默认值：1
chineseMainlandFlag
O
Integer
软件包是否分发中国大陆（不包括港澳台）。
取值范围：
0：不分发中国大陆
1：分发中国大陆
说明
当开发者注册地非中国大陆时，必须设置chineseMainlandFlag参数。
响应参数
说明
获取的上传URL有效期约为5分钟，超时后此URL将失效，需重新调用接口获取上传URL。
参数名称
必选(M)/可选(O)
类型
参数说明
ret
M
ConnectRet
包含返回码及描述信息的结果。
urlInfo
M
CommonUrlInfo
文件上传信息，包含请求方法、文件上传URL、请求头等信息。
ConnectRet
参数名称
必选(M)/可选(O)
类型
参数说明
code
O
Integer
返回码，请参见错误码。
msg
O
String
返回码描述信息。
CommonUrlInfo
参数名称
必选(M)/可选(O)
类型
参数说明
objectId
M
String
文件在文件服务器中的对象ID。
url
M
String
文件的上传URL，格式为https://{domainname}.com/{objectId}。
method
M
String
HTTP请求方法。获取文件上传地址时固定返回PUT。
headers
O
Map<String,String>
使用文件上传URL上传文件到文件服务器时，请求头所需的参数，为JSON结构，格式为：{"Authorization":"***","x-amz-content-sha256":"***","x-amz-date":"***","Host":"***","user-agent":"***","Content-Type":"***"}。
具体说明请参见上传文件接口。
调用示例
publicstaticJSONObjectgetUploadUrlForObs(String domain, String clientId, String token, String appId, String fileName, String sha256, Integer contentLength, Integer releaseType, String userAgent) {
HttpGet get = newHttpGet(domain + "/publish/v2/upload-url/for-obs?appId=" + appId + "&fileName=" + fileName + "&sha256=" + sha256 + "&contentLength=" + contentLength + "&releaseType=" + releaseType + "&userAgent=" + userAgent);
get.setHeader("Authorization", "Bearer " + token);
get.setHeader("client_id", clientId);
try {
CloseableHttpClient httpClient = HttpClients.createDefault();
CloseableHttpResponse httpResponse = httpClient.execute(get);
int statusCode = httpResponse.getStatusLine().getStatusCode();
if (statusCode == HttpStatus.SC_OK) {
BufferedReader br =
newBufferedReader(newInputStreamReader(httpResponse.getEntity().getContent(), Consts.UTF_8));
String result = br.readLine();
// Object returned by the app information query API, which can be received using the AppInfo object. For details, please refer to the API reference.
JSONObjectobject = JSON.parseObject(result);
returnobject;
}
} catch (Exception e) {
}
returnnull;
}
更新应用文件信息
更新时间: 2025-11-21 17:52
功能介绍
图片、视频、APK、RPK、AAB等文件上传完成后，通过该接口刷新应用文件信息。接口调用者的角色：账号持有者、管理员、APP管理员、运营。
接口原型
承载协议
HTTPS PUT
接口方向
开发者服务器 -> 华为服务器
接口URL
https://connect-api.cloud.huawei.com/api/publish/v2/app-file-info
数据格式
请求：Content-Type: application/json
响应：Content-Type: application/json
请求参数
Header
说明
API客户端方式和OAuth客户端方式的区别请参见使用入门。
API客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
client_id
M
String
客户端ID，获取方法参考创建API客户端。
Authorization
M
String
认证信息，格式为“Authorization: Bearer ${access_token}”。access_token为获取Token中获取的access_token。
OAuth客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
teamId
M
String
开发者所在团队的团队ID，可通过获取团队列表接口获取。
oauth2Token
M
String
认证信息，传入获取用户授权码中获取的Access Token。
Query
参数名称
必选(M)/可选(O)
类型
参数说明
appId
M
String
需要更新的应用ID。
releaseType
O
Integer
应用发布方式。
取值范围：
1：全网
3：分阶段
默认值：1
Body
请求Body中使用JSON格式携带更新的应用文件信息，参数如下表所示。
参数名称
必选(M)/可选(O)
类型
参数说明
lang
O
String(8)
语言种类，取值参见语言类型。
更新图片、视频时此参数必选。
imgShowType
O
Integer(1)
截图展现方式。
取值范围：
0：竖屏。
1：横屏。
当fileType为2时该参数必选。
videoShowType
O
Integer(1)
视频展现方式。
取值范围：
0：竖屏。
1：横屏。
当fileType为1时该参数必选，且视频及其海报实际的展示方式（横屏或竖屏）必须与videoShowType指定的方式一致。
clearArk
O
Integer(1)
是否清除方舟包（用于Android + 方舟升级到Android）。
取值范围：
1：需要清除方舟包；
0：不需要清除方舟包。
默认值：0
sensitivePermissionDesc
O
String(2048)
敏感权限描述。
fileType
M
Integer(1)
文件类型。
取值范围：
0：应用图标。
1：应用介绍视频及对应海报，最多3个视频以及对应海报，files中必须先传视频再传海报，且视频和海报必须交替上传，即先传第1组的视频再传对应的海报，再传下一组视频和海报。
2：应用介绍截图。
3：推荐视频，1个推荐视频以及对应海报。files中必须先传视频再传海报。
4：推荐图片。
5：软件包，如RPK、APK、AAB等。
6：代理证书或者代理版权图片。
7：电子证书PDF文件。尺寸不超过5MB。
8：文化部版号截图。
9：文化备案证明截图或者PDF，仅支持游戏应用。
10：VR封面图片。
11：VR应用截图。
12：VR应用推荐图片。
13：VR封面分层图片。
14：VR推荐图片4:3。
15：VR推荐图片1:1。
16：VR应用全景图。
各文件类型对应的文件上传要求，请参见应用文件要求。
files
M
List<FileInfo>
应用文件信息。
deviceType
O
Integer
设备类型。
取值范围：
4：手机
6：VR
7：手表
8：大屏
9：路由器
10：车机
更新多设备应用图片、视频时，该字段必填。
FileInfo
参数名称
必选(M)/可选(O)
类型
参数说明
fileName
O
String
文件名称，包括文件的后缀名。
fileType为5时，此参数必选。
fileDestUrl
M
String
文件在文件服务器中的对象ID。
先调用获取上传文件地址接口，然后调用上传文件接口上传文件，最后得到获取上传文件地址接口返回的objectId。
versionName
O
String
Windows应用的软件包版本号。
文件类型为exe软件包时必选。
displayName
O
String
注册表显示名称，用于唯一标识Windows应用。
文件类型为exe软件包时必选。
注意
调用更新应用文件信息接口，需确保发布国家字段不能为空，具体参见publishCountry字段。
请求示例
PUT /api/publish/v2/app-file-info?appId=10******57 HTTP/1.1
Host: connect-api.cloud.huawei.com
client_id: 4141******68
Content-Type: application/json
Authorization: Bearer *******
{
"fileType":5,
"files":{
"fileName":"PhotoPlaza.apk",
"fileDestUrl":"**716E24D17593E159601A67A7.apk"
}
}
响应参数
参数名称
必选(M)/可选(O)
类型
参数说明
ret
M
String(100)
包含返回码及描述信息的JSON字符串，格式为{"code":retcode, "msg": "description"}，retcode为返回码，description为返回码描述信息。返回码请参见错误码。
pkgVersion
O
List<String>
软件包标识。仅在添加了软件包的接口返回，当前仅支持一个。
响应示例
{
"ret": {
"code": 0,
"msg": "success"
},
"pkgVersion": []
}
调用示例
publicstaticvoidupdateAppFileInfo(String domain, String clientId, String token, String appId,
List<FileInfo> files) throws InvocationTargetException, IllegalAccessException {
HttpPut put = newHttpPut(domain + "/publish/v2/app-file-info?appId=" + appId);
put.setHeader("Authorization", "Bearer " + token);
put.setHeader("client_id", clientId);
JSONObject keyString = newJSONObject();
// Set the language.
keyString.put("lang", "zh-CN");
List<PublishFileInfo> fileInfos = newArrayList<>();
for (FileInfo fileInfo : files) {
PublishFileInfo publishFileInfo = newPublishFileInfo();
BeanUtils.copyProperties(publishFileInfo, fileInfo);
publishFileInfo.setFileDestUrl(fileInfo.getFileDestUrl());
fileInfos.add(publishFileInfo);
}
System.out.println(JSON.toJSONString(fileInfos));
keyString.put("files", fileInfos);
keyString.put("fileType", 0);
StringEntity entity = newStringEntity(keyString.toString(), Charset.forName("UTF-8"));
entity.setContentEncoding("UTF-8");
entity.setContentType("application/json");
put.setEntity(entity);
try {
CloseableHttpClient httpClient = HttpClients.createDefault();
CloseableHttpResponse httpResponse = httpClient.execute(put);
int statusCode = httpResponse.getStatusLine().getStatusCode();
if (statusCode == HttpStatus.SC_OK) {
BufferedReader br =
newBufferedReader(newInputStreamReader(httpResponse.getEntity().getContent(), Consts.UTF_8));
String result = br.readLine();
JSONObjectobject = JSON.parseObject(result);
System.out.println(object.get("ret"));
}
} catch (Exception e) {
System.out.println(e);
}
}
提交发布
更新时间: 2025-08-25 13:48
功能介绍
此接口用于提交应用审核，调用本接口前必须保证应用信息已补充完整，应用软件包已经上传。接口调用者的角色：账号持有者、管理员、APP管理员。
说明
软件包采用异步解析方式，请您在传包后等候2分钟再调用提交发布接口。
接口原型
承载协议
HTTPS POST
接口方向
开发者服务器 -> 华为服务器
接口URL
https://connect-api.cloud.huawei.com/api/publish/v2/app-submit
数据格式
请求：Content-Type: application/json
响应：Content-Type: application/json
请求参数
Header
说明
API客户端方式和OAuth客户端方式的区别请参见使用入门。
API客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
client_id
M
String
客户端ID，获取方法参考创建API客户端。
Authorization
M
String
认证信息，格式为“Authorization: Bearer ${access_token}”。access_token为获取Token中获取的access_token。
OAuth客户端方式：
参数名称
必选(M)/可选(O)
类型
参数说明
teamId
M
String
开发者所在团队的团队ID，可通过获取团队列表接口获取。
oauth2Token
M
String
认证信息，传入获取用户授权码中获取的Access Token。
Query
参数名称
必选(M)/可选(O)
类型
参数说明
appId
M
String
应用ID。
releaseTime
O
String
指定发布的UTC时间格式：yyyy-MM-ddTHH:mm:ssZZ，例如“2015-01-01T01:01:01+0800”。
说明
如果releaseTime未填写，应用审核通过后会立即上架。
remark
O
String
提审发布的备注信息，可以为空。如果填写，长度范围在10-300。
releaseType
O
Integer
应用发布方式。
取值范围：
1：全网
3：分阶段
说明
应用发布方式为分阶段发布时，要求该应用存在一个已上架的版本。
默认值：1
Body
当releaseType为3时，请求Body中phasedReleaseStartTime，phasedReleaseEndTime，phasedReleaseDescription必填，其他情况不用填写。
Body中使用JSON格式携带发布信息，参数如下表所示。
参数名称
必选(M)/可选(O)
类型
参数说明
state
O
String
此参数无需设置。
phasedReleaseStartTime
M
String(64)
分阶段发布生效开始时间，UTC时间格式：yyyy-MM-ddTHH:mm:ssZZ，例如“2015-01-01T01:01:01+0800”。
phasedReleaseEndTime
M
String(64)
分阶段发布生效结束时间，UTC时间格式：yyyy-MM-ddTHH:mm:ssZZ，例如“2015-01-01T01:01:01+0800”。
phasedReleasePercent
M
String(10)
分阶段发布百分比。
Android应用取值范围为0.00到100.00，必须精确到两位小数，不含百分号。
HarmonyOS应用取值为0.00到100.00，可以不带小数或只带一位小数，不含百分号。
phasedReleaseDescription
M
String(500)
分阶段发布说明。
isPureDetection
O
Integer
是否申请绿色应用认证。
取值范围：
0：否
1：是
sensitivePermissionIconUrl
O
String
绿色认证审核材料的objectId，isPureDetection=1时为必填项。该值为将绿色认证审核材料打成zip包，然后先调用获取上传文件地址接口，然后调用上传文件接口上传审核材料，最后得到获取上传文件地址接口返回的objectId。
isCommitSensitivePermissionTips
O
boolean
确认是否符合相关要求，true表示符合。isPureDetection=1时为必填项，且只可以填true。要求包括：
非必要的使用场景禁止桌面、锁屏和其他应用弹出窗口骚扰用户行为。
非必要的使用场景禁止发送无法删除的常驻通知。
禁止影响用户体验的广告，如抬头、强制插屏、侧边等。
禁止引导用户开启开发者选项。
禁止引导用户开启USB调试模式。
禁止通过热补丁，引入恶意行为和不符合以上条款的行为。
请求示例
POST /api/publish/v2/app-submit?appId=10****57 HTTP/1.1
Host: connect-api.cloud.huawei.com
client_id: 414*******68
Content-Type: application/json;
Authorization: Bearer *****
响应参数
参数名称
必选(M)/可选(O)
类型
参数说明
ret
M
String(100)
包含返回码及描述信息的JSON字符串，格式为{"code":retcode, "msg": "description"}，retcode为返回码，description为返回码描述信息。具体返回码请参见错误码。
响应示例
{
"ret": {
"code": 0,
"msg": "success"
}
}
调用示例
publicstaticvoidsubmit(String domain,String clientId, String token, String appId){
HttpPost put = newHttpPost(domain + "/publish/v2/app-submit?appid=" + appId);
put.setHeader("Authorization", "Bearer " + token);
put.setHeader("client_id", clientId);
try {
CloseableHttpClient httpClient = HttpClients.createDefault();
CloseableHttpResponse httpResponse = httpClient.execute(put);
int statusCode = httpResponse.getStatusLine().getStatusCode();
if (statusCode == HttpStatus.SC_OK) {
BufferedReader br =
newBufferedReader(newInputStreamReader(httpResponse.getEntity().getContent(), Consts.UTF_8));
String result = br.readLine();
JSONObjectobject = JSON.parseObject(result);
System.out.println(object.get("ret"));
}
} catch (Exception e) {
}
}
